# Optional: only needed for local preview (`bundle exec jekyll serve`).
# GitHub Pages builds the site server-side and does NOT use this file.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
